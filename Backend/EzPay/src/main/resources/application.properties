spring.application.name=EzPay

# Local Datasource settings
#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}

# Remote Datasource setting (ElepahntSQL)
spring.datasource.url=${EDB_URL}
spring.datasource.username=${EDB_USERNAME}
spring.datasource.password=${EDB_PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver
spring.sql.init.mode=always
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.minimum-idle=1

# JPA settings
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.defer-datasource-initialization=true

# swagger
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true
springdoc.api-docs.swagger-ui.path=/swagger-ui

# Logging settings
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Server port
server.port=8080

#Additional Properties
management.endpoints.web.exposure.include=*
jwt.secret=12059736528346bfhstsldpridycks695ghjdsa65f4s56d4f676h4f54sd6f